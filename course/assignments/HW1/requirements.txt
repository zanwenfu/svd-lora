# Requirements for HW (Q1 & Q2) + Jupyter
# Use inside your cs675_arm conda env:
#   pip install -r requirements.txt

# Core
numpy>=1.26,<2.0  # PyTorch 2.2.2 requires numpy < 2.0
pandas>=2.1,<3
matplotlib>=3.8,<4

# Jupyter
notebook>=7.2,<8
jupyterlab>=4.2,<5
ipykernel>=6.29,<7

# PyTorch (CPU/MPS wheels from PyPI; works on macOS arm64)
torch==2.2.2
torchvision==0.17.2

# Data I/O
openpyxl>=3.1,<4

# Optional: used earlier for dataset download (not needed if CSV is local)
kagglehub>=0.2.5
